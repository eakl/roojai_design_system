/// Physical size of a [Progress] bar. Drives track thickness and the type
/// scale used by its optional label/value text — see the `_resolve*(size)`
/// functions in `progress.dart`.
enum ProgressSize { sm, md, lg }
