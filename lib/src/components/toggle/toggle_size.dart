/// Physical size of a [Toggle]. Drives padding, text style and icon
/// extent — see the `_resolve*(size)` functions in
/// `toggle_style_resolvers.dart`. Mirrors `ButtonSize`.
enum ToggleSize { sm, md, lg }
